# REST API Каталога для Битрикс

## 📋 Оглавление

- [Обзор проекта](#обзор-проекта)
- [Основные возможности](#основные-возможности)
- [Структура проекта](#структура-проекта)
- [API Эндпоинты](#api-эндпоинты)
- [Настройка роутинга](#настройка-роутинга)
- [Установка и настройка](#установка-и-настройка)
- [Консольные скрипты](#консольные-скрипты)
- [Форматированный экспорт Excel](#форматированный-экспорт-excel)
- [Настройка почтовых событий](#настройка-почтовых-событий)
- [Требования к системе](#требования-к-системе)
- [Лицензия](#лицензия)

## Обзор проекта

Модуль `rest_catalog` для «1С-Битрикс: Управление сайтом» (редакция Бизнес) версии 25.Х предоставляет REST API для работы с каталогом товаров интернет-магазина. Модуль реализует три REST-контроллера, экспорт товаров в Excel и отправку данных по email.

## Основные возможности

### ✅ REST API
- **Получение категорий** - дерево активных категорий с дочерними элементами
- **Получение товаров в категории** - список товаров с минимальной ценой
- **Детальная информация о товаре** - характеристики, галерея, торговые предложения

### ✅ Экспорт данных
- **Экспорт в Excel XML** - форматированные файлы с гиперссылками
- **Экспорт в CSV** - простой текстовый формат
- **Отправка по email** - автоматическая рассылка выгрузок

### ✅ Особенности реализации
- **Сортировка по ID** - все списки отсортированы по возрастанию ID
- **Русская локаль** - правильная сортировка кириллических символов
- **Полные URL** - автоматическое определение протокола (http/https)
- **Форматирование Excel** - ячейки с рамками, жирные заголовки, гиперссылки

## Структура проекта

```
/export_page.php    # Экспорт всех активных товаров из инфоблока "Одежда" в форматированный Excel
(через CAdminExcelExport). 
Ппример вызова: 
	http://Ваш_Сайт/export_page.php

/export_public.php  # Экспорт всех активных товаров из инфоблока "Одежда" в форматированный Excel
(публичный доступ). 
Ппример вызова:
	http://Ваш_Сайт/export_public.php?api_key=12345&action=export&type=csv
	http://Ваш_Сайт/export_public.php?api_key=12345&action=export&type=excel_xml

/openapi.yaml                             # файл спецификации OpenAPI

/local/
├── server_configuration_example/         # Ключевые настройки сервера API модуля
├── modules/rest_catalog/                 # Основной модуль
│   ├── include.php                       # Регистрация модуля
│   ├── install/                          # Файлы установки
│   ├── lang/                             # Языковые файлы
│   └── lib/                              # Библиотеки модуля
│       ├── Api/                          # Обработчик API
│       ├── Controller/                   # Контроллеры REST API
│       ├─── Export/                      # Экспорт в Excel
│       │   ├── config_for_export.php     # Конфигурационный файл, значения задаются вручную
│       │   ├── console_export.php        # Консольный скрипт для экспорта данных в формате Excel
│       │   ├── console_export_email.php  # Консольный скрипт с отправкой на email
│       │   └── ExcelGenerator.php        # генератор кода в формате Excel 2003 XML
│       └── Rest.php                      # REST сервис для стандартного API
└── routes/
    └── api.php                           # Конфигурация маршрутов

/upload/
├── .htaccess                             # Настройка доступа к файлам
└── index.php                             # Просмотр файлов в upload
```

## API Эндпоинты

### 🔑 Аутентификация
API ключ для демо-версии: `12345`

### 📋 1. Получение списка категорий
```
GET /rest/{api_key}/rest_catalog.categories.get
```
**Пример ответа:**
```json
{
  "result": [
    {
      "ID": 10,
      "NAME": "Платья",
      "DETAIL_PAGE_URL": "https://site.ru/catalog/clothes/dresses/",
      "PICTURE": "/upload/iblock/abc/dress.jpg",
      "CHILDREN": [
        {
          "ID": 11,
          "NAME": "Вечерние",
          "DETAIL_PAGE_URL": "https://site.ru/catalog/clothes/dresses/evening/",
          "PICTURE": "/upload/iblock/def/evening_dress.jpg",
          "CHILDREN": []
        }
      ]
    }
  ]
}
```

### 🛒 2. Получение товаров в категории
```
GET /rest/{api_key}/rest_catalog.category.products.get?categoryId=10
```
**Пример ответа:**
```json
{
  "result": [
    {
      "ID": 100,
      "NAME": "Платье кружевное",
      "DETAIL_PAGE_URL": "https://site.ru/catalog/clothes/dresses/100/",
      "PICTURE": "/upload/iblock/def/dress100.jpg",
      "MIN_PRICE": 3500
    }
  ]
}
```

### 🔍 3. Детальная информация о товаре
```
GET /rest/{api_key}/rest_catalog.product.get?id=100
```
**Пример ответа:**
```json
{
  "result": {
    "ID": 100,
    "NAME": "Платье кружевное",
    "DETAIL_PAGE_URL": "https://site.ru/catalog/clothes/dresses/100/",
    "GALLERY": [
      "/upload/iblock/img1.jpg",
      "/upload/iblock/img2.jpg"
    ],
    "PROPERTIES": [
      {
        "code": "BRAND_REF",
        "name": "Бренд",
        "value": "Chanel"
      },
      {
        "code": "MANUFACTURER",
        "name": "Производитель",
        "value": "Chanel Fashion House"
      }
    ],
    "OFFERS": [
      {
        "id": 101,
        "name": "Платье кружевное (красное, S)",
        "article": "PL-100-RED-S",
        "color": "Красный",
        "size": "S",
        "price": 3500
      }
    ]
  }
}
```

## Настройка роутинга

### 🔧 Новый метод маршрутизации (с версии 23.500.0)

Начиная с версии 23.500.0 Битрикс использует новый механизм маршрутизации 
через метод `\Bitrix\Main\Application::getRouter()`. 
В проекте реализован современный подход:

#### 1. Конфигурация в `.settings.php`
```php
// /bitrix/.settings.php
'routing' => [
    'value' => [
        'config' => ['api.php'], // Подключаем файл конфигурации маршрутов
    ],
    'readonly' => true, // Защищаем настройки от изменений
],
```

#### 2. Файл конфигурации маршрутов
```php
// /local/routes/api.php
use Bitrix\Main\Routing\RoutingConfigurator;
use Bitrix\Main\Engine\Response\Json;

return static function (RoutingConfigurator $routes) {
    $routes->prefix('rest/{api_key}')
        ->where('api_key', '12345') // Проверка API ключа
        ->group(function (RoutingConfigurator $routes) {
            // Определение маршрутов
            $routes->get('rest_catalog.categories.get', function ($api_key) {
                // Обработка запроса
                return new Json($result);
            })->name('rest.catalog.categories.get');
        });
};
```

#### 3. Серверная конфигурация

**Apache (.htaccess):**
```apache
<IfModule mod_rewrite.c>
    Options +FollowSymLinks
    RewriteEngine On
    
    # Обработка авторизации
    RewriteRule .* - [E=REMOTE_USER:%{HTTP:Authorization}]
    
    # Стандартный битрикс-роутинг
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-l
    RewriteCond %{REQUEST_FILENAME} !-d

    # НОВЫЕ ПРАВИЛА ДЛЯ РОУТИНГА (С версии 23.500.0 и выше)
    RewriteCond %{REQUEST_FILENAME} !/bitrix/routing_index.php$
    RewriteRule ^(.*)$ /bitrix/routing_index.php [L]

    # ЗАКОММЕНТИРОВАТЬ старую конфигурацию через urlrewrite.php
    # RewriteCond %{REQUEST_FILENAME} !/bitrix/urlrewrite.php$
    # RewriteRule ^(.*)$ /bitrix/urlrewrite.php [L]
</IfModule>
```

**Nginx:**
```nginx
   # Обработка авторизации (передача заголовка Authorization)
   location ~* ^.+\.(php|html?|phtml)$ { 
       fastcgi_param REMOTE_USER $http_authorization;
   }
   # Стандартный битрикс-роутинг
   location / {

       # Проверка существования файла
       if (!-f $request_filename) {
          set $bitrix_rewrite 1;
       }

       # Проверка существования директории
       if (!-d $request_filename) {
           set $bitrix_rewrite 1;
       }

       # Проверка существования символической ссылки
       if (!-l $request_filename) {
          set $bitrix_rewrite 1;
       }

       # Новые правила для роутинга (с версии 23.500.0)
       if ($bitrix_rewrite = 1) {
          rewrite ^/(.*)$ /bitrix/routing_index.php last;
       }
   }
```

### Преимущества нового подхода:
1. **Чистая конфигурация** - отдельный файл для маршрутов
2. **Группировка маршрутов** - возможность использования префиксов и middleware
3. **Проверка параметров** - встроенная валидация (например, проверка API ключа)
4. **Именованные маршруты** - удобное создание ссылок
5. **Совместимость** - работает со старым и новым роутингом

## Установка и настройка

### 1. Установка модуля
1. Скопируйте папку `rest_catalog` в `/local/modules/`
2. Перейдите в административную панель Битрикс
3. Перейдите в раздел "Настройки" → "Модули"
4. Найдите модуль "REST API для каталога" и установите его

### 2. Настройка инфоблоков
Создайте инфоблоки с указанными параметрами:

#### Инфоблок "Одежда" (ID=2)
- **Символьный код:** `clothes`
- **Включить доступ через REST:** Да
- **Символьный код API:** `CatalogClothes`

**Свойства инфоблока:**
- TITLE (Строка) - Заголовок окна браузера
- KEYWORDS (Строка) - Ключевые слова
- META_DESCRIPTION (Строка) - Мета-описание
- BRAND_REF (Справочник) - Бренд
- NEWPRODUCT (Список) - Новинка
- SALELEADER (Список) - Лидер продаж
- SPECIALOFFER (Список) - Спецпредложение
- ARTNUMBER (Строка) - Артикул
- MANUFACTURER (Строка) - Производитель
- MATERIAL (Строка) - Материал
- COLOR (Строка) - Цвет
- MORE_PHOTO (Файл) - Картинки галереи
- RECOMMEND (Привязка к элементам) - С этим товаром рекомендуем
- BLOG_POST_ID (Число) - ID поста блога для комментариев
- BLOG_COMMENTS_CNT (Число) - Количество комментариев
- BACKGROUND_IMAGE (Файл) - Фоновая картинка для шаблона
- TREND (Список) - Тренды

#### Инфоблок торговых предложений (ID=3)
- **Символьный код:** `clothes_offers`
- **Включить доступ через REST:** Да
- **Символьный код API:** `ClothesOffers`

**Свойства инфоблока:**
- CML2_LINK (Привязка к товарам (SKU)) - Элемент каталога
- ARTNUMBER (Строка) - Артикул
- COLOR_REF (Справочник) - Цвет
- SIZES_SHOES (Список) - Размеры обуви
- SIZES_CLOTHES (Список) - Размеры одежды
- MORE_PHOTO (Файл) - Картинки

## Консольные скрипты

### 📤 1. Экспорт в Excel
```bash
php /local/modules/rest_catalog/lib/Export/console_export.php
```
**Результат:**
- По умрлчанию файл сохраняется в `/upload/products_export_YYYYMMDD_HHMMSS.xls`
- Автоматическое форматирование (рамки, жирные заголовки, гиперссылки)
- Сортировка по категории и названию товара

### 📧 2. Экспорт с отправкой по email
```bash
php /local/modules/rest_catalog/lib/Export/console_export_email.php admin@example.com
```
**Результат:**
- Создание Excel файла
- Отправка на указанный email
- Инструкция по настройке почтового шаблона внутри файла

## Форматированный экспорт Excel

Модуль генерирует Excel файлы в формате **SpreadsheetML (Excel 2003 XML)** с полным форматированием:

### ✅ Особенности форматирования:
1. **Ячейки с рамками** - все ячейки имеют четкие границы
2. **Жирные заголовки** - названия столбцов выделены жирным шрифтом
3. **Гиперссылки** - ссылки на товары кликабельны
4. **Настроенная ширина** - оптимальная ширина столбцов
5. **Числовой формат** - правильное отображение цен
6. **Цветовое выделение** - заголовки с серым фоном

### 📊 Экспортируемые поля:
1. **ID** - уникальный идентификатор товара
2. **Наименование товара** - полное название
3. **Категория** - полный путь (например, "Одежда / Платья / Вечерние")
4. **Ссылка на товар** - полный URL с гиперссылкой
5. **Количество предложений** - число торговых предложений
6. **Минимальная цена** - цена "от" в рублях

### 🔄 Сортировка:
- **По категории** - с учетом русского алфавита
- **По названию товара** - в рамках одной категории

## Настройка почтовых событий

### 📝 Инструкция (также включена в файл экспорта):

1. **Создание почтового события:**
   - Перейдите: Настройки → Настройки продукта → Почтовые события
   - Создайте новое событие:
     - **Тип:** Отправка выгрузки товаров
     - **Код:** `EXPORT_CATALOG_EMAIL`
     - **Название:** Выгрузка товаров в Excel
     - **Описание:** Отправка файла с выгрузкой товаров на email

2. **Создание шаблона письма:**
   ```
   Тема: Выгрузка товаров от #DATE#
   
   Текст:
   Уважаемый пользователь,
   
   Во вложении находится выгрузка товаров в формате Excel.
   
   Файл: #FILE_NAME#
   Дата: #DATE#
   Товаров: #PRODUCT_COUNT#
   Размер: #FILE_SIZE#
   
   С уважением,
   Администрация магазина
   ```

3. **Доступные переменные:**
   - `#EMAIL#` - email получателя
   - `#FILE_NAME#` - имя файла
   - `#DATE#` - дата выгрузки
   - `#PRODUCT_COUNT#` - количество товаров
   - `#FILE_SIZE#` - размер файла

4. **Проверка настроек SMTP:**
   - Настройки → Настройки продукта → Почтовые и СМС события → Настройки SMTP
   - Убедитесь, что почта настроена корректно

## Требования к системе

### 🖥️ Минимальные требования:
- **1С-Битрикс:** версия 23.500.0 или выше
- **PHP:** версия 8.2 или выше
- **MySQL:** версия 5.7 или выше
- **Модули Битрикс:**
  - iblock (Информационные блоки)
  - catalog (Торговый каталог)
  - main (Основной модуль)
  - rest (REST API)

### 📦 Рекомендуемые настройки PHP:
```ini
memory_limit = 256M
max_execution_time = 300
upload_max_filesize = 50M
post_max_size = 50M
```

## Лицензия

Проект распространяется под лицензией MIT. Смотрите файл LICENSE для подробностей.

## Поддержка

Для получения технической поддержки или сообщения об ошибках:
1. Проверьте журнал ошибок сервера
2. Убедитесь, что все инфоблоки настроены корректно
3. Проверьте права доступа к папкам
4. Проверьте настройки почтового сервера

## Вклад в проект

Приветствуются:
- Отчеты об ошибках
- Предложения по улучшению
- Пулл-реквесты с исправлениями

## Автор

Юрий Сергеев [Email](mailto:ysergeev@yandex.ru).

---
*Последнее обновление: январь 2026*